services:
  touchline:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - PORT=8080
      - DB_PATH=/data/touchline.db
    volumes:
      - touchline-data:/data

volumes:
  touchline-data:
